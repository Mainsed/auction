import FindLot from "../Components/FindLots/FindLot";
import {connect} from 'react-redux';

const mapStateToProps = (state) => {
    return {

    }
}

const CreateLotContainer = connect(mapStateToProps, {})(FindLot);

export default CreateLotContainer