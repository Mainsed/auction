import Header from "../Components/Header/Header";
import {connect} from 'react-redux';

const mapStateToProps = (state) => {
    return {

    }
}

const CreateLotContainer = connect(mapStateToProps, {})(Header);

export default CreateLotContainer