import SignUp from "../Components/LogIn/SignUp";
import {connect} from 'react-redux';

const mapStateToProps = (state) => {
    return {

    }
}

const CreateLotContainer = connect(mapStateToProps, {})(SignUp);

export default CreateLotContainer