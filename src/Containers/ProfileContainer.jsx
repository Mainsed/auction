import Profile from "../Components/Profile/Profile";
import {connect} from 'react-redux';

const mapStateToProps = (state) => {
    return {

    }
}

const CreateLotContainer = connect(mapStateToProps, {})(Profile);

export default CreateLotContainer