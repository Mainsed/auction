let initialState = {};

const createLotReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}


export default createLotReducer;

